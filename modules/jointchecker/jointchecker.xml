<module>
    <!-- module's name should match its executable file's name. -->
    <name>jointchecker</name>
    <description>Checks a given joint configuration according to  specified limits in joint and cartesian space.</description>
    <version>1.0</version>

    <!-- <arguments> can have multiple <param> tags-->
    <arguments>
    </arguments>

    <!-- <authors> can have multiple <author> tags. -->
    <authors>
          <author email="raffaello.camoriano@iit.it">Raffaello Camoriano</author>
    </authors>

     <!-- <data> can have multiple <input> or <output> tags. -->
    <data>
        <!-- input data if available -->
        <input>
            <type>Vector</type>
            <port>/generator/out</port>
            <required>no</required>
            <description></description>
        </input>
    </data>

    <dependencies>
        <computer>
        </computer>
    </dependencies>

    <!-- specific libraries or header files which are used for development -->
    <development>
        <library>YARP</library>
        <library>iKin</library>
    </development>

</module>

